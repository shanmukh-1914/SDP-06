# Copy this file to .env and set your MongoDB connection string.
# Do NOT commit your real .env file or secrets to version control.

# Example (use your Atlas connection string):
# MONGODB_URI="mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/yourdbname?retryWrites=true&w=majority"

MONGODB_URI=
PORT=4000
